# 生命树乐队官网

[https://lifetree.pro/](https://lifetree.pro/)

## 项目介绍

生命树乐队官网是一个基于 Vue 3 + TypeScript + Vite 构建的现代化音乐展示平台，提供音乐播放、视频展示、图片画廊等功能，为用户提供沉浸式的音乐体验。

## 技术栈

- **前端框架**: Vue 3.5.25
- **开发语言**: TypeScript
- **构建工具**: Vite 7.2.4
- **路由管理**: Vue Router 4.6.3
- **样式预处理**: Less 4.4.2
- **开发工具**: Vue DevTools 8.0.5

## 功能特性

### 🎵 音乐播放器
- 支持音频文件播放（MP3格式）
- 播放控制：播放/暂停、上一首、下一首
- 进度条显示与拖动控制
- 音量调节功能
- 歌曲列表循环播放
- 歌曲下载功能

### 🎬 媒体展示
- 视频播放与展示
- 图片画廊功能
- 响应式布局设计

### 💬 通知系统
- 全局消息通知组件
- iOS灵动岛风格设计
- 支持消息队列管理
- 自动消失与手动关闭

## 项目结构

```


## 开发环境要求

- Node.js: ^20.19.0 || >=22.12.0
- npm 或 yarn 包管理器

## 安装与运行

### 克隆项目
```bash
git clone [https://github.com/GEORGEWWWU/lifetree.pro]
cd vue-lifetree
```

### 安装依赖
```bash
npm install
```

### 开发环境运行
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 预览生产构建
```bash
npm run preview
```

### 类型检查
```bash
npm run type-check
```

## 组件使用说明

### 音乐播放器组件 (Player.vue)

音乐播放器是核心功能组件，提供完整的音乐播放体验。

#### 功能特性
- 播放/暂停控制
- 上一首/下一首切换
- 进度条显示与拖动
- 音量调节
- 歌曲下载
- 分享功能（开发中）

#### 使用方法
```vue
<template>
  <Player />
</template>

<script setup lang="ts">
import Player from './components/Player.vue';
</script>
```

### 通知组件 (Notification.vue)

全局通知组件，采用iOS灵动岛风格设计。

#### 功能特性
- 固定在顶部显示
- 圆滑胶囊容器设计
- 自动消失功能
- 消息队列管理

#### 全局调用方法
```javascript
// 显示通知
this.$notification.show('这是一条通知消息');

// 隐藏特定通知
this.$notification.hide(notificationId);

// 清空所有通知
this.$notification.clear();
```

## 状态管理

### 播放器状态 (player.ts)

使用Composition API实现播放器状态管理：

```typescript
import { usePlayer } from './stores/player';

const {
  currentSong,      // 当前播放歌曲
  isPlaying,        // 播放状态
  currentTime,      // 当前播放时间
  duration,         // 歌曲总时长
  volume,           // 音量
  playlist,         // 播放列表
  currentIndex,     // 当前歌曲索引
  
  // 计算属性
  formattedCurrentTime,  // 格式化的当前时间
  formattedDuration,     // 格式化的总时长
  progressPercentage,    // 播放进度百分比
  
  // 方法
  setPlaylist,      // 设置播放列表
  playSong,         // 播放指定歌曲
  togglePlay,       // 切换播放状态
  playNext,         // 播放下一首
  playPrevious,     // 播放上一首
  updateTime,       // 更新播放时间
  updateDuration,   // 更新歌曲时长
  setVolume         // 设置音量
} = usePlayer();
```

### 通知状态 (notification.ts)

全局通知状态管理：

```typescript
import { useNotification } from './stores/notification';

const {
  notifications,        // 通知队列
  currentNotification,   // 当前显示的通知
  
  // 方法
  addNotification,       // 添加通知
  removeNotification,    // 移除通知
  removeCurrentNotification,  // 移除当前通知
  clearAllNotifications  // 清空所有通知
} = useNotification();
```

## 样式规范

项目采用Less预处理器，遵循以下样式规范：

- 使用BEM命名规范
- 响应式设计，适配不同屏幕尺寸
- 统一的颜色变量和间距变量
- 模块化样式组织

## 部署说明

### 环境变量配置

创建`.env.production`文件：

```
VITE_APP_TITLE=生命树乐队官网
VITE_APP_BASE_URL=/
```

### 构建部署

1. 执行构建命令：
```bash
npm run build
```

2. 将`dist`目录下的文件部署到服务器

## 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](https://github.com/GEORGEWWWU/lifetree.pro/blob/main/LICENSE) 文件了解详情

## 联系方式

- 官网: [https://lifetree.pro/](https://lifetree.pro/)
- 邮箱: [69927088@qq.com](mailto:69927088@qq.com)

## 更新日志

### v1.0.0 (2025-12-11)
- 初始版本发布
- 实现音乐播放器核心功能
- 添加媒体展示功能
- 实现全局通知系统